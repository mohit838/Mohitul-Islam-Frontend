import SectionTitle from '@/components/Common/Layouts/SectionTitle';

const Work = () => {
  return (
    <div id="#work">
      <SectionTitle title={'Work'} />
    </div>
  );
};

export default Work;
